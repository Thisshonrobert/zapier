# TypeScript-first AI learning

The learner found the separate Python service cognitively expensive and selected TypeScript/Bun with LangGraph.js for the AI subsystem. Future lessons should spend the complexity budget on agent architecture, evidence, safety, and evaluation rather than a second application stack.
